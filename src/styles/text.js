import { StyleSheet } from 'react-native';

const text = StyleSheet.create({

	headerText: {
		fontSize: 18
	},

	buttonText: {
		alignSelf: 'center',
		color: '#007aff',
		fontSize: 16,
		fontWeight: '600',
		paddingTop: 10,
		paddingBottom: 10
	}

});

export default text;
